# Οδηγίες Διαχείρισης & Συγγραφής Περιεχομένου

Καλώς ήρθατε στη νέα δομή της τεκμηρίωσης! Αυτό το αρχείο περιέχει αναλυτικές οδηγίες για το πώς μπορείτε να προσθέτετε και να επεξεργάζεστε το περιεχόμενο για τα δύο εγχειρίδια: **Ansible Learning Path** και **Docker Automation με Ansible**, καθώς και πώς να διαχειρίζεστε τα αρχεία λήψης (downloadable files).

---

## 1. Δομή Φακέλων & Εγχειριδίων

Η υποδομή του έργου οργανώνεται στους εξής φακέλους:

- 📁 **`ansible/`**: Περιέχει το εγχειρίδιο **Ansible Learning Path** (16 Κεφάλαια με υποπαραγράφους).
- 📁 **`docker/`**: Περιέχει το εγχειρίδιο **Docker Automation με Ansible** (6 Κεφάλαια με υποπαραγράφους).
- 📁 **`files/`**: Φάκελος στον οποίο τοποθετούνται όλα τα αρχεία που θέλετε να προσφέρετε για κατέβασμα στους χρήστες (π.χ. `.yml`, `.sh`, `.zip`, `.pdf`).
- 📁 **`initial-content/`**: Αρχειοθετημένο το αρχικό περιεχόμενο/demo της θεματικής ενότητας.

---

## 2. Πώς να εισάγετε Περιεχόμενο στα `.md` Αρχεία

Κάθε κεφάλαιο βρίσκεται στον αντίστοιχο υπόφακελο `chapter-XX/` (π.χ. `ansible/chapter-01/`, `docker/chapter-02/`).

### 2.1 Δομή Front Matter
Στην αρχή κάθε `.md` αρχείου υπάρχει μια ενότητα παραμέτρων (Front Matter) ανάμεσα σε τρεις παύλες (`---`):

```yaml
---
layout: default
title: 1.1 Τι είναι το Ansible & Βασικές Έννοιες
sort: 1
---
```

- **`layout: default`**: Καθορίζει το πρότυπο εμφάνισης (δεν το αλλάζετε).
- **`title`**: Ο τίτλος της σελίδας που εμφανίζεται στο πάνω μέρος και στο πλευρικό μενού πλοήγησης (Sidebar).
- **`sort`**: Ο αριθμός που καθορίζει τη σειρά εμφάνισης στο πλευρικό μενού.

### 2.2 Γραφή Περιεχομένου με Markdown
Μετά το Front Matter, γράφετε το περιεχόμενό σας χρησιμοποιώντας απλή σύνταξη Markdown:

```markdown
# 1.1 Τι είναι το Ansible & Βασικές Έννοιες

## Εισαγωγή
Το Ansible είναι ένα εργαλείο αυτοματοποίησης...

## Παράδειγμα Playbook
```yaml
- name: Test Playbook
  hosts: all
  tasks:
    - name: Ping host
      ansible.builtin.ping:
```
```

---

## 3. Πώς να προσθέσετε Νέα Κεφάλαια ή Υποπαραγράφους

### Προσθήκη νέας υποπαραγράφου (π.χ. 1.3)
1. Μέσα στον φάκελο του κεφαλαίου (π.χ. `ansible/chapter-01/`), δημιουργήστε ένα νέο αρχείο, π.χ. `1.3-new-topic.md`.
2. Προσθέστε το Front Matter στην αρχή:
   ```yaml
   ---
   layout: default
   title: 1.3 Νέο Θέμα
   sort: 3
   ---
   ```
3. Γράψτε το περιεχόμενο της υποπαραγράφου.

---

## 4. Διαχείριση & Σύνδεση Αρχείων Λήψης (`files/`)

Όλα τα αρχεία που θέλετε να μπορούν να κατεβάσουν οι αναγνώστες τοποθετούνται στον φάκελο `files/`:

- Για το Ansible: `files/ansible/my-playbook.yml`
- Για το Docker: `files/docker/docker-compose.yml`

### Πώς συνδέετε ένα αρχείο λήψης μέσα σε ένα Markdown αρχείο:
Γράφετε έναν σύνδεσμο (link) προς το αρχείο ξεκινώντας από το `/files/`:

```markdown
[📥 Κατεβάστε το Playbook (my-playbook.yml)](/files/ansible/my-playbook.yml)
```

Ή για αρχείο Docker:
```markdown
[📥 Κατεβάστε το Docker Compose αρχείο](/files/docker/docker-compose.yml)
```

---

## 5. Τοπική Προεπισκόπηση (Local Server)

Για να δείτε πώς εμφανίζονται οι σελίδες στον περιηγητή σας:

```bash
make server
```

Ή εναλλακτικά:
```bash
npm run dev
```

---

> 💡 **Συμβουλή**: Μπορείτε ανά πάσα στιγμή να ανοίξετε τα έτοιμα πρότυπα `.md` αρχεία που έχουν δημιουργηθεί στους φακέλους `ansible/` και `docker/` και να αντικαταστήσετε το κείμενο με το δικό σας περιεχόμενο!
